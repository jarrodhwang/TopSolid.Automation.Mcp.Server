# Entities

Registered tools:

- `topsolid_list_elements` — inspection/reference.
- `topsolid_find_element` — inspection/reference.
- `topsolid_list_parameters` — inspection/reference.
- `topsolid_list_functions` — inspection/reference.
- `topsolid_list_publishings` — inspection/reference.
- `topsolid_find_named_elements` — inspection/reference.
- `topsolid_inspect_entity_structure` — inspection/reference.
- `topsolid_list_entity_children` — inspection/reference.
- `topsolid_create_entity_folders` — requires explicit confirmation.
- `topsolid_move_entities` — requires explicit confirmation.
- `topsolid_read_element_properties` — inspection/reference.
- `topsolid_inspect_elements` — inspection/reference.
- `topsolid_list_named_elements` — inspection/reference.
- `topsolid_update_elements` — requires explicit confirmation.
- `topsolid_delete_elements` — requires explicit confirmation.
- `topsolid_translate_elements` — requires explicit confirmation.
- `topsolid_set_entity_colors` — requires explicit confirmation.
- `topsolid_get_element_info` — inspection/reference.
- `topsolid_rename_element` — requires explicit confirmation.
- `topsolid_set_element_visibility` — requires explicit confirmation.
- `topsolid_translate_element` — requires explicit confirmation.
- `topsolid_get_user_selection` — inspection/reference.
- `topsolid_list_modeling_operations` — inspection/reference.

API coverage is generated from the machine-local SDK/reference cache and is not published here. See the root [validation report](../../../VALIDATION.md) for source contracts and runtime limits.
