# Parameters

Registered tools:

- `topsolid_list_parameter_values` — inspection/reference.
- `topsolid_set_parameter_values` — requires explicit confirmation.
- `topsolid_create_parameters` — requires explicit confirmation.
- `topsolid_set_parameter_value` — requires explicit confirmation.
- `topsolid_inspect_parameters` — inspection/reference.
- `topsolid_get_parameter_choices` — inspection/reference.
- `topsolid_create_parameter_expressions` — requires explicit confirmation.
- `topsolid_set_parameter_expressions` — requires explicit confirmation.
- `topsolid_get_parameter_value` — inspection/reference.

API coverage is generated from the machine-local SDK/reference cache and is not published here. See the root [validation report](../../../VALIDATION.md) for source contracts and runtime limits.
