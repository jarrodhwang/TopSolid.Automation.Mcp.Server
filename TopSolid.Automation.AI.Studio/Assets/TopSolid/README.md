These PNG files were copied without modification from the user's local collection:

`C:\Users\jarrod\Downloads\TopSolid Download\TopSolid.Icons.v7.19\Icons`

`provenance.json` records the exact source-relative path and SHA-256 of each image.
The application embeds this small selection so installed builds do not depend on the Downloads folder.
These are TopSolid vendor assets, not original artwork or a grant of redistribution rights.

The source symbol, pixel dimensions and transparency are retained. The application logo uses
`Cad\Interop\Gmi\Inventor\Deployment\Program.png`; approvals choose a target-specific project, folder,
document, part or sketch symbol before using generic action icons. CAM operation and parameter cards use
the bundled Operations Manager and operation-parameters icons from the same collection.
