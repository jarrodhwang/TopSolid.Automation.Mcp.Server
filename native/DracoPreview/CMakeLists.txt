cmake_minimum_required(VERSION 3.24)
project(TopSolidDracoPreview LANGUAGES CXX)
add_executable(TopSolid.DracoPreview main.cpp)
target_compile_features(TopSolid.DracoPreview PRIVATE cxx_std_17)
target_include_directories(TopSolid.DracoPreview PRIVATE "${DRACO_ROOT}/include")
target_link_libraries(TopSolid.DracoPreview PRIVATE "${DRACO_ROOT}/lib/draco.lib")
if(MSVC)
  target_compile_options(TopSolid.DracoPreview PRIVATE /W4 /EHsc /utf-8)
endif()
